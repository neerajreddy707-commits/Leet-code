class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        count=0
        started = False
        for i in range(len(s)-1,-1,-1):
            if  s[i]!=" ":
                    count=count+1
                    started = True
            else:
                if started:
                    break
        return count
