class Solution:
    def climbStairs(self, n: int) -> int:
        current=0
        step1=1
        step2=2
        if n<=2:
            return n
        for i in range (3,n+1):
            current = step1+step2
            step1=step2
            step2 = current
        return step2
        
